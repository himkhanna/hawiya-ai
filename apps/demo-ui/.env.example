# Copy to .env.local and fill in.
# .env.local is git-ignored (covered by node-default .gitignore).

# Hawiya AI base URL
VITE_HAWIYA_BASE_URL=http://localhost:8010

# Tenant UUID — created by `scripts/seed_dev_tenant`
VITE_HAWIYA_TENANT_ID=

# Bearer token (Phase 1 stub)
VITE_HAWIYA_BEARER=dev
